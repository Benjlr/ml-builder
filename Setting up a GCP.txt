Create GCP instance 
1 Select Australia for the region
2 Select Linux Ubuntu 18.04 Minimal
3 Ensure HTTPS Traffic is allowed
4 Add cpus, storage, and graphics cards as needed
5 Create

Setup environment
1. Start SSH shell
2. update Linux environment (sudo apt-get update)
3. get Deploy scripts from github (wget xxx) ensure you get the tar.gz version
4. run tar -zxvf xxxx on downloaded file
5. cd into untarred directory
6. run ./box_setup.sh
	if script wont run try sh box_setup.sh
7. run conda init bash (might have to restart shell)
8. run conda install -c pytorch -c fastai fastai
9. Consider copying data across :
	#COPY ACROSS DATA FOR DEMO
	gsutil cp gs://demo-sml/craigslistVehiclesFull.csv .
	gsutil cp gs://demo-sml/craigslistVehicles.csv .
10. run ./code-server -p xxx
11. Navigate to external IP and use password generated by code-server
